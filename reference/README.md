# reference/

Aquí va material de referencia, nunca código que se ejecute. Está fuera de
ESLint, de Prettier y del build.

## Falta el prototipo

El plan era dejar aquí `prototype.jsx`, el artifact `asistente-musical.jsx`,
como referencia visual y de comportamiento. **No estaba disponible al arrancar
el proyecto**, así que la fase 0 se ha construido a partir del enunciado.

Si aparece, cópialo como `reference/prototype.jsx`. Sirve como referencia de
qué enseñar y en qué orden, no como código a copiar: la arquitectura de
`docs/ARCHITECTURE.md` manda sobre él.
